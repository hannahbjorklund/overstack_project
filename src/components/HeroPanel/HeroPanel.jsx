export default function HeroPanel({category, stats}){

    console.log(stats);

    return (
        <div className='heroPanel'>
            <select>
                {
                    
                }
            </select>
        </div>
    )
}